name: Deploy Full Stack to Firebase and Cloud Run

on:
  push:
    branches:
      - walking-skeleton

jobs:
  build-and-deploy-backend:
    name: Build and Deploy Backend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v1
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v1

      - name: Configure Docker
        run: gcloud auth configure-docker us-central1-docker.pkg.dev

      - name: Build and Push Docker Image
        run: |
          docker build -t us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/api-backend/main:latest .
          docker push us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/api-backend/main:latest

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy api-backend \
            --image=us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/api-backend/main:latest \
            --region=us-central1 \
            --platform=managed \
            --allow-unauthenticated

  deploy-frontend:
    name: Deploy Frontend
    needs: build-and-deploy-backend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v1
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'

      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Deploy to Firebase Hosting
        run: |
          firebase deploy --project ${{ secrets.GCP_PROJECT_ID }} --only hosting -m "Deploy from GitHub Actions"
